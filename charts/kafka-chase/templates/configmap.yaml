---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kafka-chase.fullname" . }}
  labels:
    {{- include "kafka-chase.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "kafka-chase.config" . | nindent 4 }}
